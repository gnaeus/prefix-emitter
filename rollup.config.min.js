var uglifyPlugin = require("rollup-plugin-uglify");
var deepmerge = require("deepmerge");
var config = require("./rollup.config.js");

module.exports = deepmerge(config, {
    dest: "dist/prefix-emitter.min.js",
    
    plugins: [
        uglifyPlugin()
    ]
});